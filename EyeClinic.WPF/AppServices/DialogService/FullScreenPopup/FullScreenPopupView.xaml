<UserControl x:Class="EyeClinic.WPF.AppServices.DialogService.FullScreenPopup.FullScreenPopupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:lex="http://wpflocalizeextension.codeplex.com"
             Style="{StaticResource LocalizationStyle}"
             Background="LightBlue"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="1200">
    <DockPanel>
        <!--<Border Background="black"
                DockPanel.Dock="Bottom"
                HorizontalAlignment="Right"
                Margin="2"
                CornerRadius="12">
            <Button Content="Close"
                    VerticalAlignment="Bottom"
                    HorizontalAlignment="Right"
                    Style="{StaticResource ButtonBase}"
                    Command="{Binding CancelCommand}" />
        </Border>-->
        <ContentControl Content="{Binding ContentControl,UpdateSourceTrigger=PropertyChanged}" />
    </DockPanel>
</UserControl>
