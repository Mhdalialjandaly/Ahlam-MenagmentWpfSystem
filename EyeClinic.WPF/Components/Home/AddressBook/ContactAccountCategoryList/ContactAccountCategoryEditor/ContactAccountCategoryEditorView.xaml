<UserControl x:Class="EyeClinic.WPF.Components.Home.AddressBook.ContactAccountCategoryList.ContactAccountCategoryEditor.ContactAccountCategoryEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:EyeClinic.WPF.Components.Home.AddressBook.ContactAccountCategoryList.ContactAccountCategoryEditor"
             d:DataContext="{d:DesignInstance local:ContactAccountCategoryEditorViewModel,IsDesignTimeCreatable=True}"
             xmlns:lex="http://wpflocalizeextension.codeplex.com"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <DockPanel>
        <StackPanel Orientation="Horizontal"
                    DockPanel.Dock="Top"
                    Margin="12 4">
            <TextBlock Text="{lex:Loc CategoryName}"
                       VerticalAlignment="Center"
                       Width="180" />
            <TextBox Text="{Binding CategoryName}" />
        </StackPanel>
    </DockPanel>
</UserControl>
